{
    "name": "clipcc-extension-jdev-mochablox",
    "version": "1.0.0",
    "author": "JaydenDev",
    "scripts": {
        "build": "rimraf ./build && mkdirp build && rimraf ./dist && mkdirp dist && webpack --bail",
        "build:dist": "NODE_ENV=production npm run build"
    },
    "dependencies": {
        "clipcc-extension": "^0.1.7"
    }
}
